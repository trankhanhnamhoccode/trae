const runProjectInContainer = async () => {
  // Placeholder only. Never execute untrusted user-generated code in this MVP.
  return {
    provider: 'placeholder',
    status: 'disabled',
    message: 'Container execution is intentionally disabled for the MVP.'
  };
};

module.exports = {
  runProjectInContainer
};
